version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: latest
        commands:
            - npm --prefix frontend/ install
    pre_build:
        commands:
            - echo "prebuild $FrontendBucket"
    build:
        commands:
            - npm --prefix frontend/ run build
    post_build:
        commands:
            - echo "we are in the post build phase"
